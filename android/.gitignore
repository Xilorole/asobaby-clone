# Gradle
/.gradle
/captures/
/local.properties
.cxx/

# Keystore / signing
key.properties
**/*.keystore
**/*.jks

# Allow the shared debug keystore (not sensitive, standard password "android")
!debug.keystore
