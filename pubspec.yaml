name: asobaby
description: "Minimal app update checker using GitHub Releases."
publish_to: "none"
version: 1.1.0+2

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter

  dio: ^5.7.0
  package_info_plus: ^8.0.0
  open_filex: ^4.5.0
  path_provider: ^2.1.5
  flutter_riverpod: ^2.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
